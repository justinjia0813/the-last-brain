# 0.2.1 — 全库检索与界面还原

- 移除每次读取 20 MB、单篇 1 MB 的截断，逐篇检索全部未排除且可读取的 Markdown 笔记。
- 复用 Obsidian 读取缓存，只保留排名靠前的相关片段；扫描期间显示进度并支持停止。
- 分别显示已检索、主动排除和读取失败数量，避免将大小限制与读取错误混为一谈。
- 聊天默认在主标签页打开；恢复已确认原型的品牌侧栏、消息气泡、字体层级、深浅主题和窄窗口布局。
- 来源改为带编号跳转的抽屉，模型配置提供预设地址、模型候选与真实连接测试。
- 保留现有聊天记录、来源快照、记忆、配置及只读限制。

全库覆盖指本地 Markdown 检索范围，不包含附件解析，也不表示把整库发送给模型。发送的仍是受数量和字符预算限制的相关片段。当前采用关键词检索，不能保证同义词或语义召回。

核心与原生界面检查通过；实库验证覆盖全部 4,832 篇 Markdown，读取失败为 0。连接测试仅访问本机模拟服务，未调用真实付费模型。移动端尚未实测。

升级只替换 `main.js`、`manifest.json`、`styles.css`，保留原有 `data.json`。
