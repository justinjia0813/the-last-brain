# 0.2.2 — 作者署名与产品介绍

- 插件作者统一显示为 `justinjia0813`，并链接到作者的 GitHub 主页。
- 重新整理仓库首页，加入深浅色实机演示图，以及独立的安装、使用和隐私说明。
- 保留 0.2.1 的全库 Markdown 检索、来源引用、只读边界和对话记忆功能。

升级只替换 `main.js`、`manifest.json`、`styles.css`，保留已有对话、记忆和模型配置。
