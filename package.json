{
  "name": "the-last-brain",
  "version": "0.2.2",
  "private": true,
  "license": "MIT",
  "repository": "github:justinjia0813/the-last-brain",
  "description": "Read-only vault conversations and reviewed memory for Obsidian",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "test": "node --import tsx --test tests/*.test.ts",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
